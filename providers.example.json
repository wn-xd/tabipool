{
  "_comment": "Copy to providers.json. Each provider: own upstream + keysFile (keys-<name>.txt). 'models' lists the model ids this upstream serves, for gateways that return an empty /v1/models. Manage these from the dashboard instead of editing by hand.",
  "providers": [
    { "name": "example", "upstream": "https://api.example.com", "keysFile": "keys.txt", "enabled": true, "models": ["claude-opus-5", "claude-sonnet-4"] }
  ]
}
